# Prompt Manifest

Prompt files used by the Assignment 5 multi-agent workflow are stored in `research_agents/prompts/`.
Each agent event records the prompt path, model routing, input artifact, output artifact, tool calls, token estimate, and elapsed time in `runs/<run_id>/logs/agent_events.jsonl`.

